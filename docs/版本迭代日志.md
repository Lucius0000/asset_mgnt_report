# 版本迭代日志

## 2026-04-12
- 建立 bare Git 管理根与 `main` / `codex` worktree。
- 初始化 `src/asset_mgnt_report` 工程骨架、共享配置与指标模块。
- 开始重构旧入口为“顶部配置区 + 环境变量覆盖”模式。
- 新增 Streamlit Web UI、Docker 配置、`.env.example`、`AGENTS.md`、测试骨架与对比验证脚本。
- 新增 `.gitattributes`，统一跨设备文本换行与二进制文件处理规则。
- 完成 main / codex 核心输出对比；除加密模块短中期年化收益率与 Sharpe 因统一几何年化而变化外，其余当前纳入校验的主报表保持一致。
- 重组脚本目录为 `scripts/`、`scripts/pipelines`、`scripts/validation`，入口脚本统一收敛到 `scripts/` 根层。
- 重写 `Readme.md`，整合旧说明、工程化结构、统一口径、Web UI 与 Docker 使用方法。
- 新增 `compose.yaml` 与 `docs/docker部署与使用指南.md`，补全 Docker Web UI / CLI 双模式运行与镜像分享说明。
- 将 `版本迭代日志.md` 移入 `docs/`，并把原 `stock_us_cn_hk` 残留目录归档到 `archive/legacy_code/stock_us_cn_hk/`，进一步收敛根目录结构。
- 修复 `Dockerfile` 在 `python:3.12-slim` 下构建 `matplotlib` 所需的系统依赖，完成镜像构建与 `amr-ui` 容器启动验证。
- 补充 Docker 文档中的本机实测信息：Dockerfile 位置与体积、项目镜像占用、Docker 程序总占用、数据盘位置、当前 Docker 配置与 Dockerfile 修改规则。
- 将根目录 `assets/` 迁移到 `docs/assets/`，并清理根目录的 `__pycache__`、`.pytest_cache`、`tmp/` 等临时目录，完成第二轮结构优化收尾。
- 删除根目录 `main.py`、`Gainer.py`、`整体.py`，并将正式入口固定为 `scripts/main.py`、`scripts/gainer.py`、`scripts/overall.py`、`scripts/web_ui.py`。
- 重构 Streamlit Web UI：改为主页 + 工作台 + 结果区模型，默认启用代理，补充返回主页入口，并用统一样式重做视觉层级与交互组织。
- 修正 Web UI 导航状态管理，补充页面内返回主页入口与工作台渲染回归测试，降低出现空白页时的排查成本。
- 将 Web UI 工作台导航切换为顶部标签栏，修复点击工作台后白屏的问题；同步收缩首页 Hero 高度、修正侧边栏配色，并通过浏览器实测确认四个工作台均可正常显示。
- 将主页“工作入口”改回可点击的同页按钮展开模式，修复四个工作台卡片不可点击的问题；同时清理侧边栏空白按钮文案，并通过浏览器分别实测主页、主报表、Gainer、整体表、校验工作台的渲染结果。
- 修复 `scripts/web_ui.py` 仅导入模块导致 Streamlit 在重跑后不再渲染页面的根因问题，改为显式 `render_app()` 入口；同步补充 Web UI 回归测试，覆盖“打开主报表工作台”和“清空结果面板”后页面仍可正常渲染的场景。
- 新增 `scripts/tools/local_edge_gui.py`，将本机 Edge GUI 自动化能力收敛为可复用脚本；新增 `docs/本机GUI自动化工具.md`，记录复用方法、命令格式与当前项目的浏览器回归用法。
- 修复 Web UI 工作流运行阶段的稳定性问题：主报表、Gainer、整体表、全量校验改为后台任务启动，点击执行后页面立即返回“运行中”状态，不再因长任务同步执行表现为卡死。
- 修复整体表工作台的路径输入状态污染问题：为输入/输出/日志路径改用新的 widget 键，恢复默认路径后再执行不会再触发空路径导致的 `InvalidFileException`。
- 强制设定主页面文本输入颜色，避免路径字段在浅色输入框中不可见；补充“恢复默认路径”“刷新状态”等交互按钮。
- 扩展本机 GUI 自动化脚本和全局 `local-edge-gui` skill，新增 `wait=秒数` 步骤，便于对长任务按钮后的真实浏览器状态做同会话截图与回归验证。
- 将 Web UI 后台任务模型升级为结构化作业控制器：支持 `running / cancelling / cancelled / success / error` 状态、自动刷新、已完成模块列表、当前模块/步骤、当前子任务粗进度与日志窗口。
- 主报表接入模块级进度与取消：页面可看到当前模块、已完成模块、待执行模块；取消请求会在模块边界和股票长循环边界生效。
- Gainer 接入步骤级进度与取消：页面按“股票市值 / 债券市值 / 黄金市值 / BTC 市值”展示步骤推进，股票市值步骤内展示 `S&P 500 / HS300 / HSI` 粗粒度进度。
- 扩展 `scripts/tools/local_edge_gui.py` 与全局 `local-edge-gui` skill，新增 `fill=标签::内容` 步骤，后续可直接在本机 Edge 中为表单控件填值并执行带输入参数的 Web UI 回归。
- 修复 Gainer 工作台的前端稳定性问题：将日期输入从 `date_input` 调整为文本输入并改为后端校验，避免浏览器动态模块加载异常导致的红框报错。
- 为 Gainer 工作台补充黄金价格填写说明，加入 LBMA Gold Price 页面链接，并明确提示选择 `USD PM` 后再录入价格。
- 修复汇率模块在代理失败后的空数据链路：Eastmoney 失败时先尝试直连，再回退到 Yahoo Finance 或本地缓存；即使所有外部源都不可用，也不再触发 `single positional indexer is out-of-bounds`。
- 修复 `local-edge-gui` 的 `fill=` 步骤为“先全选清空，再输入新值”，避免带默认值的输入框被错误拼接。
- 将 Gainer 工作台恢复为可点选的日历控件，并锁定“本周末日期（周六） / 两周前日期（周六）”口径；提交前会校验两者都为周六且相差 14 天。
- CLI 侧同步更新 Gainer 日期提示与校验逻辑，保证命令行入口与 Web UI 在日期口径上保持一致。
- 汇率模块进一步重构为分层数据源：主源保留 `forex_hist_em`，备用源改为走代理环境的 `yfinance`，官方降级源接入 `currency_boc_safe` 与 `currency_boc_sina`，最后才回退本地缓存。
- 汇率原始缓存文件改为“空结果不覆盖已有缓存”，并在 `fx_metrics.xlsx` 中写出 `数据源` 字段，便于区分主源、备用源、官方降级源与缓存。
- PR 前末轮体检已覆盖 `main(currency)`、`gainer`、`overall` 三条 CLI 链路，以及 Web UI 的 Gainer 日历选择和主报表运行/取消链路；真实浏览器截图已输出到 `output/playwright/`。
- 修复汇率模块 `MoM / YoY` 缺失的问题：当 `yfinance` 只返回极短历史或被限流时，不再被误判为有效数据，而是继续降级到 `currency_boc_safe` 等官方源。
- 修复交叉汇率 `CNH_HKD` 的混合数据源问题：当 `USD_CNH` 与 `USD_HKD` 来自不同来源时，优先统一切换到同一可用来源，避免交叉汇率口径混杂。
- 逐个扫描并修复 `scripts/` 下 Python 脚本的混用换行符问题，统一为 LF，消除 Spyder 对“当前脚本文件使用了多个换行符”的提示。
- 新增 `scripts/tools/normalize_line_endings.py`，用于提交前扫描和一键修复混用换行符；同步将预防规则写入 `AGENTS.md`，要求在同步到 `wealth-hunter` 后再次复扫。
- 将 `房地产-半自动化表格.xlsx` 从 `archive/` 恢复到 `scripts/`，避免人工工作流配套文件被误认为纯归档资料。
- 更新 `Readme.md`：补充房地产半自动表格的用途说明，并将整体表入口写法统一为 `scripts/overall.py`。
